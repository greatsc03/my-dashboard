# 나의 대시보드 — 어디서나 접근 가능하게 배포하기

총 소요 시간: 약 20분 (무료)

---

## STEP 1. Supabase 가입 및 DB 생성 (5분)

1. https://supabase.com 접속 → **Start your project** → GitHub 또는 이메일로 가입
2. **New project** 클릭
   - Organization: 본인 계정 선택
   - Project name: `my-dashboard`
   - Database password: 안전한 비밀번호 입력 (메모해두기)
   - Region: **Northeast Asia (Tokyo)** 선택
3. 프로젝트 생성 완료 후 (1~2분 대기)
4. 왼쪽 메뉴 → **SQL Editor** → **New query** 클릭
5. `supabase_schema.sql` 파일 전체 내용을 붙여넣기 → **Run** 클릭
6. "Success" 메시지 확인

### Supabase 키 복사
- 왼쪽 메뉴 → **Settings** → **API**
- **Project URL** 복사 → 메모장에 저장
- **anon / public key** 복사 → 메모장에 저장

---

## STEP 2. GitHub 저장소 생성 (5분)

1. https://github.com 접속 → 로그인 (없으면 가입)
2. 오른쪽 상단 **+** → **New repository**
   - Repository name: `my-dashboard`
   - **Private** 선택 (개인 정보 보호)
   - **Create repository** 클릭
3. 저장소 URL 복사 (예: `https://github.com/내아이디/my-dashboard.git`)

### 코드 올리기 (PowerShell에서 실행)
```powershell
cd "C:\Users\sangc\OneDrive\문서\Claude_Antigravity\2026.04.24.금\personal-dashboard"
git init
git add .
git commit -m "Initial dashboard"
git branch -M main
git remote add origin https://github.com/내아이디/my-dashboard.git
git push -u origin main
```

---

## STEP 3. Streamlit Cloud 배포 (5분)

1. https://share.streamlit.io 접속 → **Sign in with GitHub**
2. **New app** 클릭
   - Repository: `내아이디/my-dashboard`
   - Branch: `main`
   - Main file path: `app.py`
3. **Advanced settings** 클릭 → **Secrets** 탭에 아래 입력:

```toml
ANTHROPIC_API_KEY = "sk-ant-api03-oZ8LyO_..."
SUPABASE_URL = "https://xxxx.supabase.co"
SUPABASE_KEY = "eyJhbGciOiJIUzI1Ni..."
```

4. **Deploy!** 클릭 → 1~3분 후 완료

---

## 완료 후

- URL 형태: `https://내아이디-my-dashboard-app-xxxx.streamlit.app`
- 이 URL을 북마크에 저장하면 **집, 회사, 스마트폰** 어디서나 접근 가능
- 데이터(목표, 업무, 번역)는 Supabase에 저장되어 영구 보존

---

## 로컬 실행도 계속 가능

- `실행.bat`으로 실행 시: 로컬 JSON 파일 사용 (기존 방식)
- Streamlit Cloud에서 실행 시: Supabase 사용 (클라우드 방식)
- 두 방식을 동시에 사용하면 데이터가 각각 따로 저장되니, 클라우드 배포 후에는 URL로만 접근하는 것을 권장합니다
